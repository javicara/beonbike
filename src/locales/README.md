# Locales

This directory contains translation files for internationalization.

## Structure

- `en/` - English translations (default)
- `es/` - Spanish translations

Each language directory contains JSON files for different sections of the application. 